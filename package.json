{
  "name": "slack-gui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tail -f /dev/null",
    "build": "mkdir -p dist && go build -o dist/slack-gui ./cmd/slack-gui",
    "build:dev": "mkdir -p dist && go build -o dist/slack-gui-dev ./cmd/slack-gui",
    "test": "go test ./...",
    "vet": "go vet ./..."
  }
}
